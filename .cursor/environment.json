{
  "name": "CompetePulse",
  "install": "corepack enable && corepack prepare pnpm@10.33.3 --activate && pnpm install --frozen-lockfile && pnpm --filter @competepulse/core build",
  "terminals": [
    {
      "name": "worker",
      "command": "pnpm --filter @competepulse/worker dev --ip 0.0.0.0 --port 8787"
    },
    {
      "name": "landing",
      "command": "pnpm --filter @competepulse/landing dev --host 0.0.0.0 --port 4321"
    }
  ]
}
